# Designer OS (extension)

Separate Chrome extension source. Kept in its own folder so it stays fully
independent from `extension/` (Durud Reminder).

Structure:
- `manifest.json` — MV3 manifest
- `popup.html` / `popup.js` / `styles.css` — toolbar popup
- `icons/` — extension icons (to be added)

Features are TBD.
